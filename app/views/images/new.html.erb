<h1>New Image</h1>

<%= simple_form_for(@image) do |f| %>
  <%= f.input :url, label: 'URL: ', placeholder: 'Enter URL to be saved' %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Back', images_path %>
