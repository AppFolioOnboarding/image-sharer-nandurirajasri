<%- flash.each do |name, message| %>
  <p class='flash'><i><%= "#{message}" %></i></p>
<% end %>

<p>
  <strong>URL:</strong>
  <%= @image.url %><br>
  <strong>Image:</strong>
  <%= image_tag @image.url %>
</p>
<%= link_to 'Back', images_path %>
